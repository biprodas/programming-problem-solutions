#include <bits/stdc++.h>
using namespace std;
#define sc(x) scanf("%d", &x)
#define scc(x, y) scanf("%d %d", &x, &y)
#define lng long long

int main(){
    int m, n;
    while(scc(m,n)==2){
        cout<<(m*n-1)<<endl;;
    }
	return 0;
}